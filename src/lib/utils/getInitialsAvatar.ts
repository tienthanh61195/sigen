import axios from "axios";

export default async function (name: string) {
  // return (await axios.get(`https://ui-avatars.com/api/?name=${encodeURIComponent(name)}&background=random`)).data
  // return (await axios.get('https://picsum.photos/200')).data
}